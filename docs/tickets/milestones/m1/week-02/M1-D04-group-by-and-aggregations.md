# Ticket: GROUP BY and Aggregations

## Milestone
M1

## Day
D04

## Goal
Use SQL in the notebook to summarize data instead of listing every row.

## Context
Aggregation is one of the first places SQL feels powerful to beginners.

## Tasks
- Open `local/learning-workspace/m1/week-02/notebooks/m1_w2_sql_basics.ipynb`.
- Add a `GROUP BY` markdown heading.
- Write at least 2 queries using `COUNT`, `AVG`, `MIN`, `MAX`, or `SUM`.
- Include at least one `GROUP BY`.
- Add one short interpretation to your notes after running the queries.

## Acceptance Criteria
- At least one grouped summary query works.
- The learner can explain what each aggregate function is doing.

## Verification
- Run the grouped query cells and inspect the results in the notebook.
