# Ticket: SELECT, WHERE, and ORDER BY

## Milestone
M1

## Day
D03

## Goal
Write your first practical filter and sort queries inside the Week 2 notebook.

## Context
These are the core SQL building blocks the learner will keep using.

## Tasks
- Open `local/learning-workspace/m1/week-02/notebooks/m1_w2_sql_basics.ipynb`.
- Add a `SELECT/WHERE/ORDER BY` markdown heading.
- Write at least 3 queries:
  - one plain `SELECT`,
  - one `SELECT ... WHERE`,
  - one query with `ORDER BY`.
- Summarize what each query is answering in `local/learning-workspace/m1/week-02/notes/d03-query-results.md`.

## Acceptance Criteria
- The notebook contains 3 readable queries.
- The note explains the business meaning of each query.

## Verification
- Run each query in the notebook and confirm output tables appear.
